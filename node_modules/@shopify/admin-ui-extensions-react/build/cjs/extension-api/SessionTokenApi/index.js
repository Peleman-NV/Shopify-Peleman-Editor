'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var extensionApi = require('@shopify/admin-ui-extensions/extension-api');
var utils = require('../utils.js');

function useSessionToken() {
  const api = utils.useExtensionApi();
  if (!extensionApi.isSessionTokenApi(api)) {
    throw new Error('No sessionToken api found');
  }
  return api.sessionToken;
}

exports.useSessionToken = useSessionToken;
