'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var extensionApi = require('@shopify/admin-ui-extensions/extension-api');
var utils = require('../utils.js');

function useData() {
  const api = utils.useExtensionApi();
  if (!extensionApi.isDataApi(api)) {
    throw new Error('No data api found');
  }
  return api.data;
}

exports.useData = useData;
