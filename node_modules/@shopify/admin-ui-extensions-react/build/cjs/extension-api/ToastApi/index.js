'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var extensionApi = require('@shopify/admin-ui-extensions/extension-api');
var utils = require('../utils.js');

function useToast() {
  const api = utils.useExtensionApi();
  if (!extensionApi.isToastApi(api)) {
    throw new Error('No toast api found');
  }
  return api.toast;
}

exports.useToast = useToast;
