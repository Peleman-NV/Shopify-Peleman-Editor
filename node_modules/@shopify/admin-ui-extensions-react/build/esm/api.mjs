import { render as render$1 } from '@remote-ui/react';
import { createElement } from 'react';
export { extend } from '@shopify/admin-ui-extensions';
import { ExtensionApiContext } from './extension-api/utils.mjs';

function render(renderCallback) {
  return (root, api) => {
    const element = renderCallback(api);
    render$1( /*#__PURE__*/createElement(ExtensionApiContext.Provider, {
      value: api
    }, element), root, () => {
      root.mount();
    });
  };
}

export { render };
